<h1>Restaurants detail</h1>
<%= @restaurant.name %>
<br>
<%= @restaurant.address %>
<% @restaurant.reviews.each do |review| %>
<li>
<%= review.content %> <%= link_to "Edit", edit_restaurant_review_path(@restaurant.id, review.id)%>
<%= link_to "Delete", restaurant_review_path(@restaurant.id, review.id), method: :delete, data: { confirm: "Are you sure you want to delete this review?"}%>
</li>
<% end %>

<%= link_to "Add review", new_restaurant_review_path(@restaurant.id)%>
<br>
<%= link_to "Back", restaurants_path %>

